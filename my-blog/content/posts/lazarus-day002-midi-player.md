---
title: "LazarusでMIDIプレイヤーを作成 - Day2"
date: 2026-01-02T10:00:00+09:00
draft: false
featured_image: "/images/LazarusDay002MenuFail.png"
description: "Lazarusチャレンジ2日目。MIDIプレイヤーを作成しました。前回作成したカエルの歌を再生できるようにするアプリを開発中です。"
tags: ["Lazarus", "MIDI", "チャレンジ", "Day002", "Pascal"]
categories: ["技術"]
author: "ブログ管理者"
---

# LazarusでMIDIプレイヤーを作成 - Day2

Lazarusチャレンジ2日目です。今回は、前回作成したカエルの歌のMIDIファイルを実際に再生できるMIDIプレイヤーを作成することにしました。

## 今回の目標

Day1でMIDIファイルを作成するアプリを完成させましたが、作成したMIDIファイルを再生する手段がありませんでした。そこで今回は、以下の機能を持つMIDIプレイヤーを開発することにしました：

- MIDIファイルの再生機能
- Trackbarによる再生位置の調整
- 再生、停止、一時停止の操作
- メニューからの操作
- ファイルを開く機能

前回作成したカエルの歌を、自分で作ったプレイヤーで再生できるようになるのが目標です。

## プロジェクトの作成

まずはLazarus IDEを開いて、新規プロジェクトを作成します。

1. Lazarus IDEを起動
2. 「ファイル」→「新規プロジェクト」を選択
3. 「アプリケーション」を選択してOK
4. プロジェクト用のフォルダを作成し、lprファイルとunitファイルを保存

プロジェクトの準備ができたら、Cursorでフォルダを開いて開発を進めます。Cursorを使うことで、AIの力を借りながら効率的にコーディングできるはずです。

## Cursorとの協働開発

Cursorに以下のプロンプトを送り、MIDIプレイヤーの実装を依頼しました：

```
MIDIファイルの再生ができるソフト。
Trackbarで再生位置調整可能。再生、停止、一時停止
メニューで操作可能。ファイルを開く。
```

Cursorは自動的に必要なコンポーネントを配置し、MIDI再生のためのコードを生成してくれました。LazarusのMIDI再生には、Windowsの場合は`mmsystem`ユニットを使用する必要がありますが、Cursorが適切に処理してくれたようです。

## ビルドと実行

コードが完成したら、コマンドラインからビルドを実行します：
こちらもCursorにプロンプトを投げます。

```
c:\Lazarus\lazbuildでビルド
```

ビルドは成功し、実行ファイルが生成されました。しかし、実行してみると...

## 発生した問題

![Menuが表示されていない状態](/images/LazarusDay002MenuFail.png)

アプリケーションを起動してみたところ、**メニューが表示されていない**という問題が発生しました。これではファイルを開く機能が使えず、MIDIファイルを再生することができません。

おそらく、メニューコンポーネント（MainMenu）の設定に問題があるか、フォームの設計時にメニューが正しく配置されなかった可能性があります。

## IDEを開いて修正

Cursorを使っていると普段はIDEは開かないのですが、今回はメニューの設定が必要なのでLazarus IDEを開きました。

「IDEはいらないのでは？Lazarusの意味はあるのか。fpcだけでよくね？」と思いながらも、フォームデザイナーでメニューを設定する必要があるのでIDEを使うことにしました。

![MenuPropertyの設定画面](/images/LazarusDay002MenuProperty.png)

画像のように、Menuになにもプロパティが設定されていないので、メニューを設定します。「ファイル」メニューと「開く」メニュー項目を追加しました。

設定が完了したらコンパイルします。

![Menuが正常に表示された状態](/images/LazarusDay002MenuOK.png)

できたー！メニューが正常に表示されるようになりました。

## カエルの歌を再生してみる

早速、昨日作成したカエルの歌のMIDIファイルを開いて再生してみます。

できたー！と思ったら、**ドしか聞こえない**。おかしい、停止してしまう。

もう一回再生ボタンを押すと「レ」。もう一回押すと「ミ」。

うーん、**一音ごとに再生がストップしてしまうみたい**。MIDIファイルの再生処理に問題があるようです。おそらく、MIDIイベントの処理方法や再生ループの実装に問題があるのでしょう。

## Cursorに再修正を依頼

非常に安直ですが、たぶんやってくれるでしょう。Cursor君がんばれ。

プロンプトを再び投げます：

```
再生ボタンを押すと一音しか再生されずにとまってしまう。再生ボタンを押したら最後まで再生するようにして。
```

修正が終わったら、再び「ビルド」とプロンプトを投げます。

先ほど「C:\Lazarus\Lazbuildでビルドして」と言っているので、Cursorはそれを覚えています。「ビルド」だけで通じます。最初に教えていないとLazbuildがパスが通っていないので見つけられません。パスを通してもいいのですが、環境変数を汚したくないので。`C:\Lazarus\`は自分がインストールした場所です。普通のインストールすればここに入っています。

## 修正結果の確認

さて、これで修正されているのでしょうか。

起動してみました。見た目は先ほどと変わっていません。

再生してみると・・・。

**だめやないかーい。**

再生すると、今度はドを再生したらトラックバーが最後にいって再生終了。Cursor君、今回はうまくいかなかったようです。

ちょっとコードを見てみるしかないか。次回は自分でコードを確認して修正する必要がありそうです。

## コードを確認してみる

コードを1分ほど、眺めてみて、うーん。

わからん。

ということでCursorにできなくてもGeminiはわかるのでは。

Geminiにすべてを任せた。

Geminiにプロンプト：

```
以下のコードは不完全で、再生を押すと、一音しか再生されない。どこが悪いか解説。
unit Unit1;
・・・コードをここにコピーした。
```

すると解説が返ってきました。

### Geminiの解説

ご提示いただいたコードで「一音（あるいは一瞬）しか再生されない」という現象が起きる最大の原因は、**TrackBar1Change イベントの再帰的な呼び出し（ループ）**にあります。

#### 根本的な原因：無限ループ（シークの競合）

Geminiの解説によると、問題の流れは以下の通りです：

1. `Timer1Timer` が実行され、`UpdatePosition` を呼び出します。
2. `UpdatePosition` 内で、現在の再生位置に合わせて `TrackBar1.Position := ...` と値を書き換えます。
3. すると、プログラムによって値が変わったにもかかわらず、`TrackBar1Change` イベントが発生してしまいます。
4. `TrackBar1Change` 内で `SetMidiPosition`（MCIのシーク操作）が実行されます。
5. 再生中にシーク操作（MCI_SEEK）を行うと、多くのデバイスでは再生が停止するか、あるいはシークした瞬間にまたタイマーが動いて位置を戻そうとするため、再生が極めて短時間で止まったり、冒頭に戻ったりしてしまいます。

結果として、「再生開始 → タイマー作動 → トラックバー更新 → シーク命令発行 → 再生停止」という流れが一瞬で起きています。

#### 解決方法

この問題を解決するには、プログラムによるトラックバーの更新と、ユーザーによる操作を区別する必要があります。具体的には：

- フラグ変数を用意して、プログラムによる更新中かどうかを管理する
- プログラムによる更新時は、`TrackBar1Change` イベント内でシーク操作をスキップする
- ユーザーがトラックバーを操作したときだけ、シーク操作を実行する

この方法により、タイマーによる自動更新では再生が停止せず、ユーザーが手動で位置を調整したときだけシークが実行されるようになります。

---

なるほど。よくわからんけど。

とりあえず動くコードが欲しい。

再びGeminiにプロンプト：

```
完全なコードを表示
```
おお、でてきました。

このコードをunit1に貼り付けます。

### Geminiのコードの改善点

Geminiが提供してくれたコードには、以下の改善が含まれていました：

1. **フラグ変数の追加**: `FUpdatingPosition` というフラグを追加し、プログラムによる更新中かどうかを管理
2. **UpdatePositionメソッドの改善**: トラックバーを更新する前にフラグを`True`に設定し、更新後に`False`に戻す
3. **TrackBar1Changeイベントの改善**: フラグが`True`のとき（プログラムによる更新中）はシーク操作をスキップし、`False`のとき（ユーザー操作）だけシークを実行

これにより、タイマーによる自動更新ではシーク操作が実行されず、ユーザーがトラックバーを操作したときだけシークが実行されるようになっています。結果として、再生が途切れることなく、最後まで連続して再生できるようになりました。

そしてコンパイルすると、**流れましたー！**

カエルの歌が最後まで途切れることなく、美しく再生されました。トラックバーも正常に動作し、再生位置を自由に調整できるようになりました。

## 完成したアプリ

最終的に、以下の機能を持つMIDIプレイヤーが完成しました：

- ✅ MIDIファイルの再生機能（最後まで連続再生）
- ✅ Trackbarによる再生位置の調整
- ✅ 再生、停止、一時停止の操作
- ✅ メニューからの操作
- ✅ ファイルを開く機能

前回作成したカエルの歌も、問題なく再生できるようになりました！

## ダウンロード

作成したアプリは以下のリンクからダウンロードできます：

[day002_MMPlayer.zip](/downloads/day002_MMPlayer.zip)

## 学んだこと

Day2の開発を通じて、多くのことを学びました：

### AIツールの使い分け

- **Cursor**: コード生成には便利だが、複雑な問題の修正には限界がある
- **Gemini**: 問題の原因を詳しく解説してくれる。コードの解説や修正案の提示に優れている
- 複数のAIツールを使い分けることで、より効果的に問題を解決できる

### 開発環境について

- Cursorは便利ですが、生成されたコードをそのまま使うのではなく、実際に動作確認をすることが重要
- Lazarusのフォームデザイナーでコンポーネントを配置する際は、表示状態を確認しながら進める必要がある
- コマンドラインからのビルドは効率的だが、IDEでのデバッグも併用すべき
- IDEを使う意味について考えさせられた。Cursorだけで開発できると思っていたが、フォームデザイナーなどIDEの機能はやはり必要

### 技術的な学び

- MIDI再生の実装は思ったより複雑。一音ずつしか再生されない問題は、MIDIイベントの処理方法を見直す必要がある
- イベント駆動型のプログラミングでは、イベントの再帰的な呼び出しに注意が必要
- プログラムによる更新とユーザー操作を区別するフラグの重要性
- ビルドコマンドのパス指定について、最初にフルパスを教えておくと、その後は短縮形で通じる。環境変数を汚さずに済む方法として有効

### 問題解決のプロセス

- Cursorに修正を依頼しても、必ずしも期待通りに修正されるとは限らない
- コードを自分で読んで理解しようとすることも重要だが、理解できない場合は別のAIツールに相談するのも有効
- 問題の原因を理解できなくても、動くコードを手に入れることで先に進めることもある

Day2にして早くも複数の問題に直面しましたが、最終的には完成させることができました。メニューの問題もMIDI再生の問題も解決し、無事にMIDIプレイヤーが完成しました！

## まとめ

Day2では、MIDIプレイヤーの開発を通じて、以下のことを経験しました：

1. **Cursorでのコード生成** - 最初のコードは生成されたが、問題が発生
2. **IDEでの修正** - メニューの設定など、IDEの機能が必要な場面
3. **Cursorでの再修正** - 修正依頼をしたが、期待通りにはいかなかった
4. **Geminiでの問題解決** - 問題の原因を解説してもらい、修正コードを取得

複数のAIツールを活用することで、最終的には問題を解決できました。AIツールは完璧ではないですが、適切に使い分けることで、効率的に開発を進められることがわかりました。

次回のDay3では、さらに新しいアプリに挑戦します！

---

*Lazarusチャレンジ Day 2/100*

